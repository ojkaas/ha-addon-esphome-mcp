# Cap below 2.0: mcp 2.x removed `mcp.server.fastmcp`, which this server imports
# (server/main.py). Without the cap a fresh build pulls 2.x and crashes on
# startup with ModuleNotFoundError. Stay on the 1.x FastMCP API.
mcp[cli]>=1.9.0,<2
uvicorn>=0.34.0
aiohttp>=3.10.0
